__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.env
.env.*
!.env.example
*.wav
*.mp3
*.log
tmp_*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
node_modules/
agents/**/.agentdock-sessions/

# Personal agent data root (layout v2) — local only
data/
# Legacy layout (pre-v2); keep ignored if still present during migration
workspace/
/pets/
/modules/
/voices/

# Host personalization packs (local only — not published with the repo)
assets/pets/**
!assets/pets/README.md
assets/voices/**
!assets/voices/README.md

# Local overrides (put real tokens / LAN URLs here)
config.local.yaml
**/config.local.yaml

# Private keys / accidental PEM dumps
*.pem
*.key
*.p12
*.jks
*.keystore
# Stable Android sideload signing (CI overwrite-install; NOT Play Store)
!clients/mobile/brand/android-sideload.p12

# ML / voice weights — do not publish (large + often non-redistributable)
*.ckpt
*.pth
*.pt
*.bin
*.onnx
*.safetensors
*.gguf
assets/voices/**/models/
voices/**/models/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.aider*
.cursor/

# Admin Vite source build cache (committed static/ is the served bundle)
runtime/admin/ui/dist/

# Local wake-word training scratch (venv + datasets)
clients/rpi/wakeword/train_workspace/

# Placeholder / never commit empty product shells
marketplace/
runtime/marketplace/
