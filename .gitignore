__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.env
.env.*
!.env.example
*.wav
*.mp3
*.log
tmp_*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
node_modules/
agents/pi-coding/package-lock.json
agents/**/.agentdock-sessions/

# Personal agent workspace / Obsidian vault (local only)
workspace/

# Local overrides (put real tokens / LAN URLs here)
config.local.yaml
**/config.local.yaml

# Private keys / accidental PEM dumps
*.pem
*.key
*.p12
*.jks
*.keystore

# ML / voice weights — do not publish (large + often non-redistributable)
*.ckpt
*.pth
*.pt
*.bin
*.onnx
*.safetensors
*.gguf
voices/**/models/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.aider*
.cursor/

# Admin Vite source build cache (committed static/ is the served bundle)
runtime/admin/ui/dist/

# Local wake-word training scratch (venv + datasets)
clients/rpi/wakeword/train_workspace/
