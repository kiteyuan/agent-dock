# Sideload / CI release signing (NOT for Google Play).
# Same key every build so APKs can overwrite-install.
storePassword=agentdock
keyPassword=agentdock
keyAlias=agentdock
# Path relative to android/app/ (after flutter create)
storeFile=../../brand/android-sideload.p12
